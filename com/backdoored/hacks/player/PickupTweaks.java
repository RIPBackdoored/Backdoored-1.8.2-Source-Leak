package com.backdoored.hacks.player;

import com.backdoored.hacks.*;
import a.a.g.b.*;
import com.backdoored.gui.*;

@b.a(name = "Pickup Tweaks", description = "Improve item picking up", category = CategoriesInit.PLAYER)
public class PickupTweaks extends BaseHack
{
    public PickupTweaks() {
        super();
    }
}
